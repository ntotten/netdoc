if locals.properties && properties.length > 0
  div.referenceTitle Properties
  each item in properties
    div.itemDetails(id=item.id)
      div.itemDetailsTitle= item.accessModifier + " " + item.name
      div.itemDetailsDescription!=item.summary
        include supportedProjects