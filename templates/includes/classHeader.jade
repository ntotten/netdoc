div.typeHeader
  div.namespace="namespace " + fullName.substr(0,fullName.lastIndexOf('.'))
  span.modifiers="TODO: Update with all the modifiers: static, public, class"
  span.typeName=name
  if (locals.father)
    span.father=father