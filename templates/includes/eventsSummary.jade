if locals.events && events.length > 0
  table
    tr
      th Events
    each item in events
      tr
        td
          span.itemName
            span.modifier= item.accessModifier + " "
            a(href="#"+item.id)=item.name
          div.itemSummary!=item.summary